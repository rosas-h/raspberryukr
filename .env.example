# Environment variables for LanceDB Search API

# =============================================================================
# REQUIRED: LanceDB Storage Configuration
# =============================================================================
# Path to your external LanceDB storage directory on the host machine
LANCEDB_HOST_PATH=./lancedb

# Path inside the container (usually no need to change)
LANCEDB_PATH=/app/storage/lancedb
LANCEDB_URI=/app/storage/lancedb

# =============================================================================
# REQUIRED: Embedder Configuration (choose one)
# =============================================================================
# Option 1: Use local embedding model (slower, uses more memory)
EMBEDDER_MODEL_NAME=lang-uk/ukr-paraphrase-multilingual-mpnet-base

# Option 2: Use remote embedder service (recommended for production)
# EMBEDDER_URL=http://embedder-service:80

# =============================================================================
# QA Table Configuration
# =============================================================================
# Name of the QA table in LanceDB
QA_TABLE_NAME=ukr_qa_chunked


# =============================================================================
# Search Configuration
# =============================================================================
# Default number of results to return
DEFAULT_TOP_K=5

# =============================================================================
# Server Configuration
# =============================================================================
# Port to expose the API on the host
API_PORT=8001

# Debug mode (true/false)
DEBUG=false

# =============================================================================
# Example Configurations
# =============================================================================

# Example 1: Production with remote embedder
# LANCEDB_HOST_PATH=/data/lancedb
# EMBEDDER_URL=http://text-embeddings-inference:80
# QA_TABLE_NAME=ukr_qa
# API_PORT=8000

# Example 2: Development with local embedder
# LANCEDB_HOST_PATH=./storage/lancedb
# EMBEDDER_MODEL_NAME=BAAI/bge-m3
# QA_TABLE_NAME=ukr_qa_test
# API_PORT=8001
# DEBUG=true
